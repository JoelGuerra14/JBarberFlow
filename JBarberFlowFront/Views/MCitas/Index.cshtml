@model IEnumerable<ReservaCitasBackend.Modelos.MCitas>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ID_Cliente)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ID_Estilista)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ID_Servicio)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FechaInicio)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FechaFin)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IsCanceled)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ID_Cliente)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ID_Estilista)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ID_Servicio)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FechaInicio)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FechaFin)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IsCanceled)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ID_Citas">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ID_Citas">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ID_Citas">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
